import { Check } from 'lucide-react'

export default function WeeklyTaskItem({ task, completed, onToggle, disabled }) {
  return <li><label className={`flex cursor-pointer items-start gap-3 rounded-md border p-2.5 transition-colors focus-within:ring-2 focus-within:ring-brand ${completed ? 'border-emerald-200 bg-emerald-50/60 dark:border-emerald-800 dark:bg-emerald-950/40' : 'border-slate-100 hover:bg-slate-50'}`}><input type="checkbox" checked={completed} onChange={() => onToggle(task.id)} disabled={disabled} className="peer sr-only" aria-label={`Mark ${task.title} as ${completed ? 'incomplete' : 'complete'}`} /><span className={`mt-0.5 grid size-5 shrink-0 place-items-center rounded border ${completed ? 'border-emerald-500 bg-emerald-500 text-white dark:border-emerald-400 dark:bg-emerald-600' : 'border-slate-300 bg-white'}`}>{completed && <Check size={13} aria-hidden="true" />}</span><span className="min-w-0"><span className={`block text-sm font-medium ${completed ? 'text-slate-500 line-through dark:text-slate-300' : ''}`}>{task.title}<span className="sr-only">{completed ? ' (completed)' : ''}</span></span>{task.description && <span className={`mt-0.5 block text-xs leading-snug ${completed ? 'text-slate-500 dark:text-slate-400' : 'text-muted'}`}>{task.description}</span>}{task.dueDay && <span className={`mt-1 block text-[11px] font-medium ${completed ? 'text-emerald-700 dark:text-emerald-300' : 'text-brand'}`}>Due {task.dueDay}</span>}</span></label></li>
}
