import { BookOpen } from 'lucide-react'
import Card from '../ui/Card'

export default function CourseAssignmentsCard({ assignments }) {
  return <Card className="overflow-hidden"><header className="flex items-start gap-3 border-b border-slate-100 p-6"><span className="grid size-9 place-items-center rounded-lg bg-blue-50 text-blue-600"><BookOpen size={18} /></span><div><h2 className="font-semibold">Course Assignments</h2><p className="mt-1 text-xs text-muted">Workload allocation for the current term</p></div></header><div className="overflow-x-auto px-6 pb-2"><table className="w-full min-w-[520px] text-left"><thead><tr className="border-b border-slate-100 text-[11px] uppercase tracking-wide text-muted"><th className="py-3">Course</th><th>Section</th><th>Instructor</th><th className="text-right">Maximum assigned hours</th></tr></thead><tbody>{assignments.map(assignment => <tr className="border-b border-slate-50 last:border-0" key={`${assignment.course}-${assignment.section}`}><td className="py-4 font-semibold text-slate-800">{assignment.course}</td><td><span className="rounded bg-slate-100 px-2 py-1 text-xs font-medium">{assignment.section}</span></td><td className="text-slate-600">{assignment.instructor}</td><td className="text-right"><b>{assignment.maxHours}</b><span className="ml-1 text-xs text-muted">hrs/week</span></td></tr>)}</tbody></table></div></Card>
}
